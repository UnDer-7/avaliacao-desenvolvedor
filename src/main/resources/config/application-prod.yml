logging:
    level:
        ROOT: info
spring:
    datasource:
        type: com.zaxxer.hikari.HikariDataSource
        driver-class-name: org.postgresql.Driver
        url: jdbc:postgresql://localhost:5432/avaliacao_desenvolvedor
        username: postgres
        password: root
        platform: postgres
    jpa:
        database: postgresql
